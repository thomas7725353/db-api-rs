interface:
  display_name: "DBApi Demo CRUD"
  short_description: "Seed demo CRUD and qb-list APIs into db-api"
  default_prompt: "Create or refresh the db-api demo CRUD group, datasource, demo_items table, and CRUD/qb-list API configs, then verify with curl."
